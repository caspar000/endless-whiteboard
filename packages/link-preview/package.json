{
	"name": "@lifeboard/link-preview",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"//exports": "Two entry points on purpose: '.' is isomorphic (types and the parser, no imports at all) so the browser and a future Tauri adapter can use it, './node' reaches for node: builtins and must never be pulled into the bundle.",
	"main": "./src/index.ts",
	"types": "./src/index.ts",
	"exports": {
		".": "./src/index.ts",
		"./node": "./src/node.ts"
	},
	"scripts": {
		"typecheck": "tsc -p tsconfig.json --noEmit",
		"test": "vitest run",
		"build": "tsc -p tsconfig.json --noEmit"
	},
	"devDependencies": {
		"@types/node": "^26.1.2",
		"typescript": "5.9.3",
		"vitest": "^4.1.10"
	}
}
